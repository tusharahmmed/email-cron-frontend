export enum tagTypes {
  auth = "auth",
  quote = "quote",
  user = "user",
  driver = "driver",
  customer = "customer",
}

export const tagTypesList = [
  tagTypes.auth,
  tagTypes.quote,
  tagTypes.user,
  tagTypes.driver,
  tagTypes.customer,
];
